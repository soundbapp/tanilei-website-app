/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Allow local /public images only — add external domains here if needed
    domains: [],
  },
}

module.exports = nextConfig
